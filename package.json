{
  "name": "docker-node",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist --presets latest",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "babel-preset-latest": "^6.16.0",
    "body-parser": "^1.15.2",
    "express": "^4.14.x",
    "request": "^2.79.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0"
  }
}
